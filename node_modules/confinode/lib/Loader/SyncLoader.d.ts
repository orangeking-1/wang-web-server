import Loader from './Loader';
export default abstract class SyncLoader implements Loader {
    load(fileName: string): Promise<unknown | undefined>;
    abstract syncLoad(fileName: string): unknown | undefined;
}
